package enemies;

import javax.swing.*;
import java.awt.image.BufferedImage;

public class Asteroid  {

    public Asteroid(Renderer renderer, int x, int y, BufferedImage enemyTexture) {
     //   super(renderer, x, y, enemyTexture);
    }
}
