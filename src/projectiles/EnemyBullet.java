package projectiles;

import java.awt.image.BufferedImage;

public class EnemyBullet extends Projectile{
    public EnemyBullet(int x, int y, BufferedImage bulletImage) {
        super(x, y, bulletImage);
    }
}
